Re-record this demo from the live Feedback app:

```bash
cd docs/demos/_harness
npm install
node run.mjs quickrecord-ios
```

The walkthrough drives production UI with live JamBase events and real concert clips. Auth writes are mocked so recording does not follow/upload as a real user.
