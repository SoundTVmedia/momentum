/** Premium presale is not launched yet — hide until the feature ships. */
export default function PremiumPresaleAlert(_props?: {
  artistName?: string;
  eventDate?: string;
  venueName?: string;
  presaleUrl?: string;
  presaleEnds?: string;
}) {
  return null;
}
